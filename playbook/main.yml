- name: Deploy War Files to JBoss
  hosts: 3.109.5.198
  become: yes

  tasks:
    - name: Read configuration from config.yml
      include_vars:
        file: Ansible-2/config.yml
    
    - name: Include ansible-role-jboss-deploy
      include_role:
        name: ansible-role-jboss-deploy  # Specify the name of the role to include

